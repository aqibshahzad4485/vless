# Stealth VPN Configuration

# 1. Connection Domain (Optional)
# Leave empty to use auto-detected IP (REALITY Mode)
# Set to a valid domain (e.g. vpn.example.com) to enable Auto-TLS (LetsEncrypt)
DOMAIN=

# 2. Access Control
# Block users from accessing private LAN IPs (Recommended: true)
BLOCK_LOCAL_ACCESS=true

# 3. Auto-Deletion (Idle Timeout)
# Time in hours before an idle user is deleted
IDLE_TIMEOUT_HOURS=3

# 4. VPN Port (Optional)
# Default is 443. Change if you need a specific port.
VPN_PORT=443

# 5. API Port (Optional)
# Default is 8000.
API_PORT=8000

# 6. Manual API Key (Optional)
# If not set, one is generated automatically
API_TOKEN=
